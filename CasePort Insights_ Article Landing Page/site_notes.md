# CasePort.io Live Site Notes

The live site at www.caseport.io is currently a fresh WordPress installation with the default GeneratePress theme. It has only the default "Hello world!" post. This means the Insights page will be a standalone HTML page that follows the brand system prompt exclusively, without needing to match an existing live design. The brand system prompt PDF is the authoritative design reference.
