// Shared CasePort data namespace.
// The source data files (ported verbatim into ./_src) populate this single
// live object exactly as the browser globals did — guaranteeing the data is
// byte-identical to the approved source. Typed facades live in ./index.ts.
export const CP: any = {};
