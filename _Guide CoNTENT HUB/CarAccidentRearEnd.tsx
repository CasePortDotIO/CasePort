// Temporarily disabled - will be refactored to use CategoryGuideTemplate
/*
import GuideTemplate from '@/components/GuideTemplate';
import guideCategories from '@/data/guideData';

export default function CarAccidentRearEnd() {
  const guide = guideCategories.find(g => g.id === 'car-accident-rear-end');
  if (!guide) return <div>Guide not found</div>;
  return <GuideTemplate guide={guide} />;
}
*/

export default function CarAccidentRearEnd() {
  return <div>Coming soon</div>;
}
