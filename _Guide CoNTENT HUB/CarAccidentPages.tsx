// These components are temporarily disabled due to type mismatches
// They will be refactored to use the new CategoryGuideTemplate

/*
// CarAccidentTBone.tsx
import GuideTemplate from '@/components/GuideTemplate';
import guideCategories from '@/data/guideData';

export function CarAccidentTBone() {
  const guide = guideCategories.find(g => g.id === 'car-accident-t-bone');
  if (!guide) return <div>Guide not found</div>;
  return <GuideTemplate guide={guide} />;
}

// CarAccidentHitAndRun.tsx
export function CarAccidentHitAndRun() {
  const guide = guideCategories.find(g => g.id === 'car-accident-hit-and-run');
  if (!guide) return <div>Guide not found</div>;
  return <GuideTemplate guide={guide} />;
}

// CarAccidentIntersection.tsx
export function CarAccidentIntersection() {
  const guide = guideCategories.find(g => g.id === 'car-accident-intersection');
  if (!guide) return <div>Guide not found</div>;
  return <GuideTemplate guide={guide} />;
}

// CarAccidentParkingLot.tsx
export function CarAccidentParkingLot() {
  const guide = guideCategories.find(g => g.id === 'car-accident-parking-lot');
  if (!guide) return <div>Guide not found</div>;
  return <GuideTemplate guide={guide} />;
}

// CarAccidentMultiVehicle.tsx
export function CarAccidentMultiVehicle() {
  const guide = guideCategories.find(g => g.id === 'car-accident-multi-vehicle');
  if (!guide) return <div>Guide not found</div>;
  return <GuideTemplate guide={guide} />;
}
*/
